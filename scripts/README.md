# Sidebar 自动化生成工具

这个工具可以自动扫描你的 Markdown 文件并生成 VitePress 的 sidebar 配置，支持实时监听文件变化。

## 功能特性

- 🚀 **自动扫描**: 基于文件系统结构自动生成 sidebar
- 📝 **智能标题提取**: 从 Markdown 文件的第一个 `#` 标题提取文章标题
- 🔄 **实时监听**: 开发时自动监听文件变化并更新配置
- ⚙️ **灵活配置**: 通过 JSON 配置文件自定义行为
- 📁 **多层级支持**: 支持扁平结构和多层级分类结构

## 快速开始

### 1. 生成 sidebar 配置

```bash
# 手动生成一次
npm run sidebar

# 或者直接运行脚本
node scripts/generate-sidebar.js
```

### 2. 开发时自动监听

```bash
# 启动文件监听器
npm run sidebar:watch

# 或者直接运行脚本
node scripts/watch-sidebar.js
```

### 3. 构建时自动生成

构建命令已经集成了 sidebar 生成：

```bash
npm run build  # 会先生成 sidebar 再构建
```

## 配置说明

配置文件位于 `scripts/sidebar-config.json`：

```json
{
  "articleRoot": "../Article",              // 文章根目录（相对于 scripts 目录）
  "sidebarPath": "../.vitepress/configs/sidebar.mts",  // sidebar 配置文件路径
  "sections": {                             // 各个分区的配置
    "/InterviewQ/": {
      "title": "面试题",
      "subSections": {                      // 子分类配置
        "Frontend": "前端",
        "Backend": "后端",
        "TheWayofCode": "代码通用"
      }
    },
    "/Lives/": {
      "title": "生活感悟",
      "subSections": null                   // null 表示扁平结构
    },
    "/Tips/": {
      "title": "技巧分享",
      "subSections": null
    }
  },
  "titleExtraction": {                      // 标题提取配置
    "useFirstHeading": true,                // 使用第一个 # 标题
    "fallbackToFilename": true,             // 回退到文件名
    "camelCaseToSpaces": true               // 驼峰命名转空格
  },
  "sorting": {                              // 排序配置
    "enabled": true,                        // 启用排序
    "locale": "zh-CN"                       // 排序语言环境
  },
  "watch": {                                // 监听配置
    "debounceDelay": 1000,                  // 防抖延迟（毫秒）
    "fileExtensions": [".md"]               // 监听的文件扩展名
  }
}
```

## 目录结构

工具会按照以下结构扫描文件：

```
Article/
├── InterviewQ/           # 面试题分区
│   ├── Frontend/         # 前端子分类
│   │   ├── PageCloseAnalytics.md
│   │   └── FirstScreenOptimization.md
│   ├── Backend/          # 后端子分类
│   │   └── Go.md
│   └── TheWayofCode/     # 代码通用子分类
│       └── GitFlow/
├── Lives/                # 生活感悟分区（扁平结构）
│   ├── AdviceToMyPastSelf.md
│   └── FromStuckToBreakthrough.md
└── Tips/                 # 技巧分享分区（扁平结构）
    ├── AIToolsConfig.md
    └── EslintPrettierConfig.md
```

## 生成的配置格式

工具会生成如下格式的 sidebar 配置：

```typescript
export default {
  '/InterviewQ/': [
    {
      text: '前端',
      collapsed: false,
      items: [
        { text: '页面关闭时发送统计数据的最佳实践', link: '/InterviewQ/Frontend/PageCloseAnalytics' },
        { text: '首屏优化', link: '/InterviewQ/Frontend/FirstScreenOptimization' }
      ]
    },
    {
      text: '后端',
      collapsed: false,
      items: [
        { text: '如果32位机器上的int64类型', link: '/InterviewQ/Backend/Go' }
      ]
    }
  ],
  '/Lives/': [
    { text: '如果回到过去，我会这样告诫我自己', link: '/Lives/AdviceToMyPastSelf' },
    { text: '学不进去到主动突破', link: '/Lives/FromStuckToBreakthrough' }
  ]
}
```

## 标题提取规则

1. **优先使用 Markdown 标题**: 提取文件中第一个 `# 标题`
2. **回退到文件名**: 如果没有找到标题，使用文件名
3. **驼峰转换**: 将 `PageCloseAnalytics.md` 转换为 `Page Close Analytics`

## 常见问题

### Q: 如何添加新的分区？

在 `sidebar-config.json` 的 `sections` 中添加新的配置：

```json
{
  "sections": {
    "/NewSection/": {
      "title": "新分区",
      "subSections": null  // 扁平结构
    }
  }
}
```

### Q: 如何修改文章标题？

有两种方式：

1. **修改 Markdown 文件**: 在文件开头添加 `# 你的标题`
2. **修改文件名**: 重命名文件，工具会自动转换驼峰命名

### Q: 监听器不工作怎么办？

1. 检查 `Article` 目录是否存在
2. 确保有 `.md` 文件
3. 查看控制台错误信息

### Q: 如何自定义排序？

修改 `sidebar-config.json` 中的 `sorting` 配置：

```json
{
  "sorting": {
    "enabled": false,     // 禁用排序，保持文件系统顺序
    "locale": "en-US"     // 或使用其他语言环境
  }
}
```

## 开发建议

1. **开发时**: 使用 `npm run sidebar:watch` 启动监听器
2. **构建前**: 运行 `npm run sidebar` 确保配置最新
3. **新增文章**: 直接创建 `.md` 文件，工具会自动检测
4. **重构目录**: 修改配置文件后重新运行生成脚本

## 扩展功能

如果需要更多功能，可以修改脚本：

- 支持更多文件格式
- 添加文章摘要提取
- 支持自定义排序规则
- 添加文章分类标签
- 生成文章索引

---

这个工具让你专注于写作，而不用手动维护 sidebar 配置！ 🎉